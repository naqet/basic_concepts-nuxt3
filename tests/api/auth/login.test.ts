import { describe, it } from "vitest";

describe("login", () => {
	it.todo("checks if body is present", () => { });
	it.todo("checks if username and password values are valid");
	it.todo("returns 404 if no user found");
	it.todo("when user found, returns 200 with proper JWT");
});
